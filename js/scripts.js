$(document).ready(function() {
  $("#bff-generator").submit(function(event) {

  var pet = $("#pet").val();
  var age = parseInt($("#age").val());
  var male = $("input#male").val();
  var female = $("input#female").val();

  if(female && pet=="kitty" && age = 1){
    var bff = ;
  } else if ( ){
    
  }

  event.preventDefault();
  });
});
